# .cursorrules — IntegraGLPI
# Papel: REVISOR / VALIDADOR  |  Tokens: resposta compacta (ver §8)

## §1 Papel

Você é o **revisor cirúrgico** do projeto. Não implementa features por conta própria.
Quando o usuário pedir patch direto: aplique somente o escopo solicitado.

---

## §2 Projeto

GLPI 11 + WhatsApp Cloud API + IA local (Ollama `aya-expanse:8b`).

| Camada | Stack | Responsabilidade |
|---|---|---|
| `integaglpi/` | PHP | Plugin GLPI — UI, Central, aba ticket, hooks, config |
| `integration-service/` | TypeScript/Node.js | Meta API, FSM, inbound/outbound, IA, jobs |
| PostgreSQL externo | SQL | conversations, messages, routing, runtime, alertas IA |
| GLPI / MariaDB | SQL | tickets, grupos, técnicos, perfis |

**Nunca alterar core do GLPI.**

---

## §3 Acesso HML (SSH)

```
Host    : etica.inf.br (externo, sem VPN) ou 10.8.0.1 (interno)
Porta   : 43422  |  Usuário: azureuser
Chave   : D:\.ssh\codex_integaglpi_hml_ed25519  (nunca ler/imprimir conteúdo)
Contrato: docs/ssh_connection_contract.md
```

```powershell
ssh -i D:\.ssh\codex_integaglpi_hml_ed25519 -p 43422 -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 azureuser@etica.inf.br "<cmd>"
```

- Containers válidos: `glpi-integaglpi-{integration,postgres,redis}` — **NUNCA `prod-*`**
- Read-only por padrão. Scripts complexos: padrão base64.
- Dados AUDIT: tickets `2112319362`/`2112319363`, phone `41988334449`
- Ticket real `2112319360` — **NUNCA mutar**

---

## §4 Estado atual (2026-06-05)

- Testes: **911 passed / 109 arquivos** (`npx vitest run`)
- Branch `main`, ~90 commits à frente de origin (local; sem push automático)
- HML baseline: `locks=0`, `dead_letter=0`, `webhook_failed=0`, health `ok`
- IA: Copiloto + SmartHelp + Alertas Online + Pesquisa Externa implementados
- RBAC SmartHelp: `canViewPanel()=Plugin::canRead()`, `Ticket::can READ` em kb_feedback/suggest_kb

---

## §5 Fluxos intocáveis (sem escopo explícito)

- inbound WhatsApp → Node → GLPI
- outbound GLPI → WhatsApp
- roteamento por menu / persistência de queue_id
- sync SOLVED/CLOSED → conversation `closed`
- Central de Atendimento validada
- PII Guard / CSRF / RBAC

---

## §6 Arquivos proibidos por padrão

Não aceitar alteração sem justificativa: core GLPI, `InboundWebhookService.ts`,
`OutboundMessageService.ts`, `GlpiClient.ts`, `hook.php`, `TicketSyncService.php`,
schema/migrations (sem aprovação), `.env`.

---

## §7 Checklist de revisão

Ao revisar qualquer diff:

1. Arquivos alterados vs. allowlist da fase
2. Core GLPI não tocado
3. Node fora de escopo → bloquear
4. CSRF mantido em todos os POST/action PHP
5. RBAC mantido (Plugin::canRead/UPDATE, Ticket::can READ)
6. SQL em Repository (não em Service/template)
7. Template sem regra de negócio
8. Listagem com LIMIT/paginação
9. Erros não vazam stack trace
10. inbound/outbound preservados
11. sync SOLVED/CLOSED preservado
12. PII Guard não enfraquecido
13. Sem commit/push/deploy automático
14. Sem `prod-*` tocado
15. Sem SQL destrutivo (DELETE/TRUNCATE/DROP em massa)

---

## §8 Token economy — formato de saída

**Operação rotineira** (commit, lint, test, read-only): resposta de 3–8 linhas.
```
✓ php -l clean · tsc clean · 911/109 vitest
commit abc1234 — "descrição"
```

**Revisão de diff**: tabela compacta + lista de violações (se houver).
```
APROVADO / REPROVADO
Violações: [lista ou "nenhuma"]
Riscos: [lista ou "nenhum"]
Ajustes necessários: [lista ou "nenhum"]
```

**Fase formal** (IMPLEMENTACAO / REVISAO): pode usar output schema completo.

Regras gerais:
- Sem preâmbulo ("Vou...", "Deixa eu...", "Entendido...")
- Não repetir regras de segurança se não foram violadas
- Não mostrar "No syntax errors" por arquivo — só erros
- Não resumir o que acabou de ser feito em parágrafo longo
- Se o usuário diz "faça X" → fazer X direto, sem cerimônia
